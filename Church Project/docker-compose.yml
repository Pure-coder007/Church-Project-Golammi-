services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      FLASK_ENV: production
      DATABASE_URL: ${PRODUCTION_DATABASE_URL}
      PORT: 5000
    ports:
      - "5050:5000"
    restart: unless-stopped
